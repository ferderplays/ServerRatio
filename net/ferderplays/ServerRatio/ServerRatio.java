package net.ferderplays.ServerRatio;

import net.ferderplays.ServerRatio.commands.*;
import net.ferderplays.ServerRatio.commands.ratecommand.*;

public class ServerRatio {

    public ratecommand();

    void commands() {
        commands = new ArrayList<String>();
        commands.add(ratecommand);
        var1[1] = commands;
        if (this.mc.message = commands()) {
            this.mc.player.sendtitle("thanks for your rating :)");
            for (var2 = 0) {
                var2 = 0;
            }
        } 
    }
}